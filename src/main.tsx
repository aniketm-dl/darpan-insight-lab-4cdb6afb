import { createRoot } from "react-dom/client";
import App from "./App.tsx";
import "./index.css";

createRoot(document.getElementById("root")!).render(<App />);

// Dispatch event for prerender
document.dispatchEvent(new Event("render-event"));
